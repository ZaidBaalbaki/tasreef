name: easy_exchange
description: A currency exchange rate app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  
  http: ^1.3.0
  decimal: ^2.3.3
  rxdart: ^0.27.7
  provider: ^6.1.2
  fl_chart: ^0.65.0
  intl: ^0.18.1
  cupertino_icons: ^1.0.8
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  assets:
    - lib/assets/images/
